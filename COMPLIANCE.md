# Compliance Matrix

This document maps superCPE behavior to the 2026 Statement on Standards for
CPE Programs (`docs/2026-Statement-on-Standards-for-CPE-Programs.pdf`) and
nothing else. Each row ties a Standards locator to the feature that satisfies
it, the code that implements it, and any known gap. Rows are added as features
ship; an empty table means no Standards-touching feature has shipped yet.

| Locator | Requirement | Feature | Where in code | Gap |
|---------|-------------|---------|---------------|-----|
| 3.01.1 | "Learning activities provided by CPE program sponsors must specify the knowledge level, content, and learning objectives … Knowledge levels consist of Basic, Intermediate, Advanced, Update, and Overview." | 002 | `backend/app/constants/knowledge_levels.py`; validation rule 9 in `backend/app/services/packages.py`; CHECK constraint in `backend/app/models/lesson_package.py` | Ingest enforces the five levels on stored lessons. The participant-facing surface that communicates level, content, and objectives now exists (004's public course page) but is unreachable until a course is published (008). |
| 3.02.1 | "All programs identified as Intermediate, Advanced or Update must clearly identify prerequisite education, experience, and advance preparation in precise language … For courses with a program knowledge level of Basic and Overview, prerequisite education or experience and advance preparation, should be noted if applicable, otherwise, state 'none' in the course announcement or descriptive materials." | 002 | Validation rule 10 in `backend/app/services/packages.py`; `LEVELS_REQUIRING_PREREQUISITES` and `PREREQUISITES_NONE` in `backend/app/constants/knowledge_levels.py` | superCPE refuses blank prerequisites/advance preparation for Intermediate, Advanced, Update and stores the literal "None" for blank Basic/Overview. It cannot judge whether stated prerequisites are "precise language"; that is the content reviewer's call (feature 008). The course announcement that displays them is 004's public course page, unreachable until publish (008). |
| 3.01 | "Sponsored learning activities must be based on relevant learning objectives and outcomes assigned to the learning activities that clearly articulate the professional competence that should be achieved by participants." | 004 | `course_objectives` in `backend/app/services/courses.py` derives a course's objectives from its lesson packages, grouped by lesson in position order; they are shown per lesson on the admin course page and in the public 8.01 payload (`backend/app/routers/courses.py`) | Objectives are authored in video-tool and cannot be edited in superCPE; a course with wrong objectives is fixed by re-exporting the lesson. Not participant-reachable until publish (008). |
| 3.02.1 | "CPE program sponsors must develop and execute learning activities in a manner consistent with the learning objectives, prerequisite education, experience, and advance preparation of participants" — stated per course, precisely. | 004 | Attach and update-version refuse a package whose `field_of_study`, `knowledge_level`, `prerequisites`, or `advance_preparation` disagrees with the course's, naming the field and both values (`_check_agreement` in `backend/app/services/courses.py`); the agreed value is stored on `courses` and re-copied on every attach | The admin cannot type course-level values at all, so the catalog can never contradict the lessons. Page unreachable until publish (008). |
| 7.01.1 | "For learning activities in which segments are classified in multiple fields of study, the CPE credits granted should first be computed based on the content time of the total program. Next, the CPE credits granted should be allocated to the fields of study based on the field of study content time." | 004 | Attach refuses a package whose `field_of_study` differs from the course's (`backend/app/services/courses.py`) | Deliberate gap: superCPE refuses multi-field courses rather than allocating credit across fields. A course has exactly one field of study until there is a reason to build the allocation. |
| 8.01, 8.01.1 | "CPE program sponsors must provide descriptive materials that enable participants to assess the appropriateness of learning activities … must make the following information available in advance: 1) Learning objectives 2) Type of formal learning program 3) Recommended CPE credit and recommended field of study 4) Prerequisites 5) Program knowledge level 6) Advance preparation 7) Program description …" | 004, 005, 016 | Public `GET /api/v1/courses` and `GET /api/v1/courses/{course_code}` (`backend/app/routers/courses.py`, published courses only) and the `/courses/:code` page (`frontend/src/pages/CoursePage/`) carry title, description, objectives by lesson, level, prerequisites, advance preparation, field of study, and lessons with durations; 005 added item 3's `recommended_credit` with its `credit_basis` line (`public_credit` in `backend/app/services/credit.py`), served as null — and the page row omitted — below the minimum awardable, while a stale credit refuses the whole course render (016); 016 finished the payload shape: item 2 as `program_type` (the `PROGRAM_TYPE` constant, "Self study" and never "QAS Self Study"), items 8–10 as named policy links with effective dates, item 11 present only while claimable | The 011 `/policies` page remains the canonical home of the full policy texts; the course payload links them rather than inlining. |
| 8.01.2 | "All programs must clearly identify prerequisite education, experience, and advance preparation requirements, if any, in the descriptive materials. Prerequisites, if any, must be written in precise language so that potential participants can readily ascertain whether they qualify for the program." | 004, 016 | The public course payload and page always carry `prerequisites` and `advance_preparation` (never omitted; "None" is stated per 3.02.1), sourced from the packages the course's lessons agree on; 016's completeness check fails items 4 and 6 when the stored value is blank — "None" is a stored statement, a blank is not — and the publish gate refuses (`backend/app/services/disclosure.py`) | Whether the language is "precise" is the content reviewer's judgment at sign-off (008). |
| 7.02.7 | "If the entire self study program constitutes a video, then the prescribed word count formula … would consist of the actual video time plus the total number of review questions … multiplied by 1.85, divided by 50" — the duration term must be "the actual audio/video duration time (not narration of the text)". | 002 | `backend/app/services/ffprobe.py`; validation rules 4–5 in `backend/app/services/packages.py`; `duration_source = 'measured'` CHECK in `backend/app/models/lesson_package.py`; boot check in `backend/app/main.py` | The duration is measured by ffprobe on the server against the file that was uploaded, and a manifest claiming anything but `measured` is refused. superCPE cannot verify video-tool's claim that the narration inside the file was rendered from measured audio; it relies on that attestation and refuses packages that do not make it. Credit computation from this duration is a later feature. |
| 9.02.1(8), 9.02.2(7) | "Program materials." — among the required documentation elements CPE program sponsors must retain as evidence of compliance (9.01: for a minimum of five years). | 002 | `lesson_packages` stores the manifest, questions, and transcript verbatim (`backend/app/models/lesson_package.py`); the video is retained at `video_key` via `backend/app/storage.py` | Retention is currently a local disk under `backend/uploads/`; five-year durability (DigitalOcean Spaces, backups) is not implemented yet. Nothing is ever deleted, but nothing enforces the retention period either. |
| 9.01 items 1, 8, 9, 10, 11 | Certificates of completion must include "1) CPE program sponsor name … 8) NASBA sponsor identification number 9) State registration number, if required by the state boards 10) NASBA time statement stating that CPE credits have been granted on a 50-minute hour 11) Any other statements required by boards of accountancy". | 003, 010 | `sponsor_profile` and `sponsor_state_registrations` in `backend/app/models/sponsor.py`; `TIME_STATEMENT` and `CERTIFICATE_SPONSOR_FIELDS` in `backend/app/constants/certificate.py`; `missing_fields()` gates issuance (010: the `for_issuance` view — `name` and `legal_name` only — is what actually gates a render; `registry_status` gates only the item-8 claim); admin editing via `backend/app/routers/admin_sponsor.py`; 010: `certificate_snapshot` on `completions` freezes items 1–11 at completion (`create` in `backend/app/services/completions.py`); `render` in `backend/app/services/certificates.py` reads the snapshot only; item 8 present only when `may_claim_registry` was true at completion; `PROGRAM_TYPE` is "Self study", never "QAS"; 011: the vendored DejaVu Sans fonts (`backend/app/assets/fonts/`) render participant names beyond Latin-1 unchanged (010 sanitized them to replacement characters) | superCPE is not on the National Registry, so item 8 cannot be satisfied today: `registry_status` is `not_registered`, and the rule is that while it is, `national_registry_id` must be empty (CHECK constraint plus validation) and no response, page, or document may include the words "National Registry" or a sponsor ID; every feature that renders sponsor facts reads `may_claim_registry`, and the certificate tests assert the words appear nowhere. Delivery "as soon as possible … not exceed 60 days" is reported (`certificates_overdue` in `backend/app/services/readiness.py`), not enforced; email delivery is 018. Location (item 5) printed as not applicable (self study). |
| 9.01.1 | "The entity whose name appears on the certificate of completion is responsible for awarding the CPE credits earned by a participant. … For self study … a certificate supplied by the CPE program sponsor after satisfactory completion of a qualified assessment." | 003, 010 | `legal_name` on `sponsor_profile` (`backend/app/models/sponsor.py`) records the responsible entity; 010: `sponsor_legal_name` in the snapshot is the awarding entity printed on the certificate, and the certificate exists only downstream of a passed qualified assessment (`create` runs inside the passing `submit` transaction) | — |
| 9.02 | "CPE program sponsors must retain adequate documentation (electronic or paper) for a minimum of five years to support their compliance with these Standards and the reports that may be required of participants." | 003, 009, 010, 011 | Nothing is ever deleted (packages from 002, the sponsor profile here); 009: accounts are deactivated (`deactivated_at` in `backend/app/models/account.py`), never deleted, there is no delete path in code, and `sessions` and `course_reviews` FK RESTRICT to `accounts`; 010: `enrollments`, `completions`, `review_answers`, `lesson_progress`, and `attempts` FK RESTRICT to accounts and packages, completions have no update path beyond the one-time render fields and no delete path, and `delete_course` refuses while any enrollment exists; 011: `RETENTION_YEARS` in `backend/app/constants/retention.py` (9.02 quoted beside it), `retain_until` in `backend/app/services/retention.py`, shown on the admin completions table and in every bundle record that has a `completed_at` | Nothing deletes at the boundary; retention is a floor and the system keeps everything. Durable storage (Spaces, backups) is 012. |
| 7.01 | "Sponsored learning activities are measured by actual program length, with one 50-minute period equal to one full CPE credit … rules and regulations of state boards of accountancy may differ on acceptable increments … 3)(i) Traditional … Self Study: A minimum of one-fifth or one-half credit may be awarded initially … credits may be awarded in one-fifth increments". Sponsors "may round down (but not up)". | 005 | `MINUTES_PER_CREDIT`, `CREDIT_INCREMENT`, `MIN_AWARDABLE` in `backend/app/constants/credit.py`; `round_down` in `backend/app/services/credit.py` floors to one-fifth uniformly and returns 0.0 below the minimum awardable | One-fifth is applied uniformly; the per-jurisdiction increment policy state boards may require is roadmap 019 (comment at `round_down`). |
| 7.02 | "For self study programs, CPE credit must be based on one of the following educationally sound and defensible methods: Method 1: Pilot test … Method 2: Computation using the prescribed word count formula". | 005 | Method 2 is the implementation: `compute` in `backend/app/services/credit.py`; nothing accepts a typed or pilot-tested credit | Deliberate gap: Method 1 (7.02.1–7.02.4) is not implemented at all — superCPE does not pilot test and refuses to hold a half version of it. A pre-program assessment does not exist in the package contract, so its exclusion from the computation is vacuous today. |
| 7.02.5 | "The prescribed word count formula begins with a word count of the number of words contained in the text of the required reading … and should exclude any material not critical to the achievement of the stated learning objectives … Review questions, exercises, qualified assessment questions … should not be included in the word count." | 005 | Only the manifest's `word_count` enters the word term (`compute` in `backend/app/services/credit.py`); the stored transcript is never counted, and questions are counted separately in the question term, never as words | Gap: `word_count` is trusted from the video-tool manifest. superCPE cannot verify that number against the package text, nor that video-tool excluded non-critical material; the attestation is recorded in the stored manifest. |
| 7.02.6 | "[(# of words/180) + actual audio/video duration time + (# of questions × 1.85)] /50 = CPE credit … The total number of review questions (including those above the minimum requirements), exercises, and qualified assessment questions … the CPE credits granted must be rounded down." | 005 | `compute`/`store` in `backend/app/services/credit.py` with the NASBA numbers in `backend/app/constants/credit.py`; questions of both kinds are counted from each package's stored `questions` JSON; the award and its per-lesson inputs are stored on `courses`; every course mutation through `touch` recomputes, and derived staleness (`is_stale`) refuses stale use | Adaptive-path averaging (7.02.6 second paragraph) is out of scope: superCPE has no adaptive programs. Terms are truncated at two decimal places of a minute so the retained record re-adds exactly and can never round a credit up. |
| 7.02.7 | "If audio and video segments … constitute additional learning … the actual audio/video duration time may be added … If the entire self study program constitutes a video … there would be no word count for text used in the formula." | 005 | Per lesson, `av_is_additional_learning` selects the branch in `compute` (`backend/app/services/credit.py`): true counts the ffprobe-measured `duration_seconds` in the A/V term; false counts `word_count` in the word term and the duration not at all. The all-video case falls out as a zero word term | The flag is video-tool's attestation from the manifest (002 rule); superCPE cannot judge whether the audio is in fact additional learning rather than narration. |
| 9.02.2(2)(ii) | For method 2, "the word count formula calculation, as well as the supporting documentation for the data used in the word count formula (for example, word count; number of review questions, exercises, and qualified assessment questions; duration of audit or video segments … and actual calculation), must be retained." | 005 | `credit_breakdown` on `courses` stores one row per lesson with every input (duration, flag, words, questions by kind); `from_stored` and `as_text` in `backend/app/services/credit.py` rebuild the written-out calculation from the stored columns alone, without the packages | The record is retained in the database but not yet exported: the audit bundle that hands it to a reviewer is feature 011. |
| 5.01.2 | "Participant engagement for self study programs. Examples of content related participant engagement in self study programs include but are not limited to: 1) Instructional strategies, such as interactive content … 2) Content Reinforcement tools with feedback as required by 5.01.2.1, such as review questions or simulations. 3) Qualified Assessments as required by 6.01.2 …" | 006 | The player (`frontend/src/components/Player/`) asks review questions inside the video at their block's measured end; questions are normalized per package version into `questions`/`choices` (`backend/app/models/question.py`, `normalize` in `backend/app/services/questions.py`, written in the ingest transaction) | Engagement item 3, the qualified assessment, is feature 007. The player is reachable only through the admin preview until accounts (009) and enrollment (010) exist. |
| 5.01.2.1 | "Review questions or other content reinforcement tools must be placed throughout the program in sufficient intervals to allow the participant the opportunity to evaluate the material that needs to be re-studied. At least three review questions or other content reinforcement tools with scored responses per CPE credit must be included. … 'True or false' questions do not count toward the number of required review questions per CPE credit. There is no minimum passing rate required" — with the one-fifth chart (0.2→0, 0.4→1, 0.5→2, 0.6→2, 0.8→3, 1.0→3) applied again above each full credit. | 006 | Placement: `after_block` indexes into the manifest's measured `video.blocks` (contract rule 18 in `backend/app/services/packages.py`) and the player pauses at each block's `end_seconds`. Minimums: chart and `required_review_questions` in `backend/app/constants/question_minimums.py` (the above-one-credit decomposition is documented as an interpretation); two-choice questions excluded via `COUNTING_MIN_CHOICES`. `check` in `backend/app/services/readiness.py` reports `review_minimum` (block), `review_placement` and `review_two_choice` (warn); nothing scores review answers and any answer continues playback. | "Other content reinforcement tools" (simulations, exercises) are not modeled; only multiple-choice review questions can satisfy the floor. Findings are only reported here; 008 turns block findings into a publish refusal. |
| 5.01.2.2 | "Feedback must be provided on review questions or other content reinforcement tools. At a minimum, feedback must indicate that a response was 'correct' or 'incorrect'. The goal of providing feedback is to reinforce understanding, highlight knowledge gaps, and provide additional resources for learning comprehension." | 006 | Grading is server-side only (`grade_review` in `backend/app/routers/player.py`): every answer returns the verdict, the package's non-blank feedback text (contract rule 17), and the correct choice key; the player always shows verdict and feedback before the participant can continue. The answer key never reaches the browser — `tests/test_player.py` walks the play and admin payloads asserting it. | Feedback quality beyond "non-blank" is the content reviewer's judgment (008). Nothing persists which answers were given (010). |
| 6.01 | "CPE program sponsors must maintain a process to … verify individual successful program completion for self study … programs to assign the correct number of CPE credits. Self-certification of attendance/completion alone is not sufficient." | 007 | Attempts are graded and stored server-side (`backend/app/services/assessment.py`, `attempts`/`attempt_answers` in `backend/app/models/attempt.py`); nothing accepts a self-certified result — a passed attempt exists only because the server graded the submitted answers against the stored key. | Attempts are keyed to an opaque preview identity, not a participant; 009/010 add accounts and enrollment, and nothing assigns CPE credit yet (010). |
| 6.01.1 | Program completion verification for group programs: attendance records, monitoring ratios, attendance prompts. | 007 | Deliberate gap: not implemented. superCPE offers self study programs only (6.01.2's branch); nothing in the codebase claims group delivery. | Becomes real only if group programs are ever offered; none are planned. |
| 6.01.2 (passing grade) | "must require participants to successfully complete a qualified assessment during or after the program with a cumulative minimum passing grade of at least 70 percent before issuing CPE credit". | 007 | `PASSING_PCT` in `backend/app/constants/assessment.py`; `grade` in `backend/app/services/assessment.py` compares the exact ratio (correct × 100 ≥ 70 × total), never the rounded display score; each attempt snapshots `passing_pct` so the record stays provable if the constant changes. | Credit issuance itself is 010; this feature only produces the passed attempt the credit will rest on. |
| 6.01.2 (question minimums) | "At least 5 questions and scored responses per CPE credit … For example, the qualified assessment for a 5-credit course must include at least 25 questions … a 5 ½ credit course must include at least 29" — with the one-fifth chart (0.2→2, 0.4→3, 0.5→4, 0.6→4, 0.8→5, next full credit→5). | 007 | `ASSESSMENT_PER_CREDIT`, `ASSESSMENT_MINIMUMS`, and `required_assessment_questions` in `backend/app/constants/question_minimums.py`; the paragraph's own worked examples (5→25, 5½→29) are reproduced by test. `assessment_minimum` (block) in `backend/app/services/readiness.py` compares counting questions against the requirement, and `assessment.start` refuses while any block finding exists. | Findings become a publish refusal in 008. Question formats other than multiple choice (essay, simulations) are not modeled. |
| 6.01.2 (duplicates) | "Except in courses in which recall of information is the learning strategy, duplicate review and qualified assessment questions are not allowed." | 007 | `normalized_stem` in `backend/app/services/questions.py` (lowercase, whitespace collapsed, trailing punctuation stripped); `assessment_duplicate` (block) in `backend/app/services/readiness.py` names both question keys and their lessons. | The recall-as-learning-strategy exemption is not modeled; every course is held to the no-duplicates rule. Stems that paraphrase rather than repeat are the content reviewer's judgment (008). |
| 6.01.2 (forced choice) | "Forced choice responses such as 'True or false' or 'yes or no' questions are not permissible on the qualified assessment." | 007 | `MIN_CHOICES_ASSESSMENT` in `backend/app/constants/question_minimums.py`; ingest refuses assessment questions with fewer than 3 choices (validation in `backend/app/services/packages.py`, aliased to the same constant), and `assessment_forced_choice` (block) in `backend/app/services/readiness.py` keeps the check as defense in depth. | None. |
| 6.01.2 (objective coverage) | "A qualified assessment must measure a representative number of the learning objectives for the program. A representative number … is 75 percent or more." | 007 | `OBJECTIVE_COVERAGE_PCT` in `backend/app/constants/assessment.py`; `objective_coverage` (block) in `backend/app/services/readiness.py`, keyed by (package_id, objective id), listing the uncovered objectives by lesson. | The below-75-percent allowance exists only for randomized test banks, which superCPE does not use, so it is not modeled. Whether the questions actually "test the achievement" of the objectives they cite is the content reviewer's judgment (008). |
| 6.01.2 sub-ii (feedback) | Without a test bank, "on a failed assessment, the CPE program sponsor may not provide feedback to the test taker. … on assessments passed successfully, CPE program sponsors may choose to provide participants with feedback." | 007 | `result` in `backend/app/services/assessment.py` is the single source of everything a participant sees about an attempt: a passed attempt carries the per-question record, a failed one carries score, counts, and the retake affordance — no per-question data of any kind. The proof is `test_failed_result_payload_has_no_feedback` in `backend/tests/test_assessment.py`, which walks the whole payload; the assessment UI is a form submitted once, with no verdicts anywhere while an attempt is open. | superCPE serves no test bank (every question, every time), so sub-ii-a is deliberately unimplemented. |
| 9.02.2(1) | "Records of program completion verification by individual participant, including the number of CPE credits earned by participant and course completion date." | 007, 010 | Every attempt is retained with its answers, score, threshold snapshot, timestamps, and `package_versions` (which package versions were asked), so the record can prove what was asked and how it was graded even after a re-export (`backend/app/models/attempt.py`; admin view at `GET /api/v1/admin/courses/{code}/attempts`); 010: a participant's attempts carry `enrollment_id`, and `completions` records the participant (via the enrollment), `credit_awarded`, and `completed_at`, never deleted; `review_answers` and `lesson_progress` retain the engagement record beside it. | Export is 011. |
| 4.01 | "Course documentation must contain the most recent publication, revision, or review date. Courses in subjects that undergo frequent changes … must be reviewed and revised, as necessary, by a subject matter expert as soon as possible but at least once a year … Other courses must be reviewed and revised, as necessary, at least every two years." | 008, 016 | `last_documented_date` and `review_due_at` in `backend/app/services/development.py`, derived from `published_at`, review dates, and the stored `review_cycle` (365/730-day constants in `backend/app/constants/review_cycle.py`); disclosed in the public payload (`backend/app/routers/courses.py`) and on the course page — 016 asserts the field by key-set test and closed the ROADMAP improvement note; overdue reviews raise the `review_due` warn finding in `backend/app/services/readiness.py` | Enforcement of currency is reporting only: an overdue review warns here and 011 reports it; nothing blocks a published overdue course or reminds anyone. The fix is unpublish → review → republish. |
| 4.01.1 | "Learning activities must be developed by subject matter expert(s). The content developer must be competent and current in the subject matter … If technology is used in the development of the program, the content developer is responsible for reviewing the content for accuracy." | 008 | `developer_id` and `developer_used_technology` on `courses` (default true: superCPE content is drafted with a language model in video-tool, and the developer of record is the human who directed and checked that draft); `subject_matter_experts` in `backend/app/models/sme.py`; `developer_missing` block finding gates publish; developer name and credentials disclosed publicly | The developer's competence and credentials are recorded as stated, not verified against any state board (said on the `/admin/smes` page). |
| 4.02 | "…reviewed by content reviewers other than those who developed the programs … before the first presentation of these materials and again after each significant revision. The participation of at least one licensed CPA (in good standing and holding an active license … in a U.S. jurisdiction) is required in the development of every program in accounting and auditing. The participation of at least one licensed CPA, tax attorney, or IRS enrolled agent … is required in the development of each program in the field of study of taxes. … Whether to have this individual involved during the development or the review process is at the CPE program sponsor's discretion." | 008 | `course_reviews` snapshots `content_updated_at_reviewed`; `current_review` in `backend/app/services/development.py` derives whether the approved review still reviews the current content; `review_missing`, `reviewer_is_developer`, and `cpa_participation` block findings in `backend/app/services/readiness.py` (fields and credentials in `backend/app/constants/participation.py`; either role satisfies participation); the publish gate (`publish` in `backend/app/services/courses.py`) refuses on any block finding, and published courses are immutable (`_refuse_if_published`), so revision forces unpublish → edit → re-review; 009: `recorded_by_account_id` on `course_reviews`; reviewers enter reviews at `POST /api/v1/review/courses/{code}/reviews` behind `require_role("reviewer", "admin")` | "Significant revision" is approximated as *any* content change: every `touch` supersedes the review, because superCPE cannot judge significance. The international-taxes equivalence (an "active" licensed CPA of the international jurisdiction) is not modeled. Licenses recorded, not verified. The reviewer's login is not their qualification; the SME record is. Nothing verifies that the person holding the account is the person named in the SME record. |
| 4.02.1 | "Individuals or teams qualified in the subject matter must review programs. … In rare circumstances, it may be impractical to review certain programs in advance. In those rare circumstances … the basis for the lack of content review must be documented." | 008 | `impractical_basis` on `course_reviews`, entered on the record-review form and shown in the review history; its presence is reported, never used to bypass the publish gate | Whether the reviewer is "qualified in the subject matter" is the admin's recorded judgment when naming the reviewer; nothing enforces it. |
| 9.02.2(4) | "Author/instructor, author/developer, and content reviewer, as applicable, names and credentials. For the CPA and tax attorney acting … the state of licensure, license number, and status of license should be maintained. For the enrolled agent … information regarding the enrolled agent number should be maintained." | 008 | `subject_matter_experts` stores name, credentials, jurisdiction, license number, and status; reviews and courses reference SMEs with `ondelete=RESTRICT` and `delete_sme` in `backend/app/services/smes.py` refuses deletion while named, so the record cannot be orphaned; license numbers never appear in a public payload (`PublicPerson` carries name and credentials only, proven by test); 009: who recorded each review is retained beside the reviewer's name and credentials (`recorded_by` snapshots the account's email, `recorded_by_account_id` the account) | Retained in the database; the audit bundle that hands it to a reviewer is 011. Claims recorded as stated, not verified. |
| 6.01 | "Self-certification of attendance/completion alone is not sufficient." | 009, 010 | `accounts` and `sessions` (`backend/app/models/account.py`), argon2id hashes and server-side revocable sessions in `backend/app/services/auth.py`, `require_role` in `backend/app/auth.py` — the server-vouched participant identity completion verification hangs on; 010: a completion exists only as a row created inside the passing `submit` transaction (`submit` in `backend/app/services/assessment.py`, `create` in `backend/app/services/completions.py`), keyed to a participant account through the enrollment — nothing accepts a self-certified completion | — |
| 9.02.2(3) | "Course documentation must include an expiration date (the time by which the participant must complete the qualified assessment). For individual courses, the expiration date is no longer than one year from the date of purchase or enrollment." | 010 | `ENROLLMENT_DAYS` in `backend/app/constants/enrollment.py` (the paragraph quoted beside it); `expires_at` computed and stored at `enroll` (`backend/app/services/enrollments.py`); `assessment.start_for_enrollment` refuses past it and `submit` past it abandons the open attempt unscored | One year uniformly; the longer "series of courses to achieve an integrated learning plan" allowance is not modeled. The expiry is a cap, never extended — a lapsed participant needs a new enrollment with its own year. |
| 6.01.2 (re-takes) | "The number of re-takes a participant is permitted to take is at the sponsor's discretion." | 010 | `RETAKES_ALLOWED` in `backend/app/constants/assessment.py` (007's constant, now the per-enrollment re-take count) enforced at `assessment.start_for_enrollment` by counting failed attempts on the enrollment; preview attempts are never counted | The retake count is a policy choice; 011's policies page must state it. |
| 4.04, 4.04.1 | "CPE program sponsors must employ an effective means for evaluating learning activity quality … Evaluations, whether written or electronic, must be solicited from participants … for the overall program" — determining whether (1) objectives were met, (2) prerequisites were appropriate, (3) materials including the qualified assessment were relevant, (4) time allotted was appropriate, (5) instructors were effective, where applicable. | 011 | `evaluations` in `backend/app/models/evaluation.py` with the five columns (item 5 constrained null: self study has no instructors, and the null visibly answers it as not applicable); the prompts as asked are code-versioned in `backend/app/constants/evaluation.py` and quoted in the audit bundle; solicited on the result page and `/my/courses` for `SOLICIT_UNTIL_DAYS` (30, ours) after completion (`solicit`/`submit` in `backend/app/services/evaluations.py`) | Solicited, not required: a participant may decline, and nothing withholds the certificate. Instructor evaluation is not applicable and the record says so. |
| 4.04.2 | "CPE program sponsors must periodically review evaluation results to assess program effectiveness and should inform developers and instructors of evaluation results." | 011 | `evaluation_reviews` (dated, by account, `summary_snapshot` as of the review, `informed_developer` attestation), written by `record_review` in `backend/app/services/evaluations.py`; the `evaluation_review_due` warn finding (`readiness.check` and `readiness.launch_findings`) after `EVALUATION_REVIEW_DAYS` (90, ours) | "Periodically" is 90 days by our choice; reported, not enforced. Informing the developer is an attestation, not an email (018) — the admin summary page names the developer of record. |
| 4.05.3 | "At a minimum, instructional materials must include the following items: 1) An overview of topics … 4) Instructions to participants regarding navigation through the course, course components, and course completion" | 011 | Item 1: `outline` in the public course payload (`public_detail` in `backend/app/routers/courses.py`), rendered as "What this course covers"; item 4: `/how-it-works` from `backend/app/services/instructions.py`, every number read from the constant that enforces it, linked from `/my/courses` and the course page and included in the audit bundle | Items 2 (index/keyword search) and 3 (glossary) are not built; the ROADMAP improvement note names the course-package contract change both repos need. Items 5 and 6 are 006 and 007. |
| 8.01 items 8–11, 8.01.1 | "8) Course registration and, where applicable, attendance requirements 9) Refund policy for courses sold for a fee or cancellation policy 10) Complaint resolution policy 11) Official NASBA sponsor statement, if an approved NASBA sponsor" — and 8.01.1: policies "must be formalized, published, and made available to participants". | 011, 016 | `policy_versions` in `backend/app/models/policy.py` (append-only, effective-dated; current is derived, never marked); public `GET /api/v1/policies` and `/policies` page behind the same gate as the catalog; the course page links it; the re-take policy is rendered from `RETAKES_ALLOWED`/`PASSING_PCT` (`retake_policy_text` in `backend/app/services/policies.py`) so it cannot disagree with the code; `NASBA_SPONSOR_STATEMENT` in `backend/app/constants/certificate.py` rendered only under `may_claim_registry`; `site_open_blockers` in `backend/app/services/site.py` refuses opening the site while any of the three kinds has no current version; 016 added the registration/attendance policy (already one of 011's three kinds) to the course page as a named `registration_policy` link beside `refund_policy` and `complaint_policy` (each a `PolicyLink` with the current version's effective date, never inlined text), made items 8–10 publish blockers through the disclosure check, and extended the open gate to also require a published course that discloses completely | Item 11 cannot be true until registration; the constant exists and is gated, and the payload simply lacks the key meanwhile (proven by test). |
| 9.02.2 (1)–(7) | The seven required documentation elements for self study programs. | 011 | `build` in `backend/app/services/audit_bundle.py` assembles the set as one zip, element by element (`ELEMENT_MAP` quotes each and the bundle's README maps them to directories); every export is stored and logged append-only in `audit_exports` | Method 1 records (2)(i) are absent by design (005 uses only method 2). Videos are included by reference (storage key) unless requested. |
| 9.02.2(5) | "Results of program evaluations." | 011 | `5-evaluations/` in the bundle: every row with the prompts quoted, the current summary, and the 4.04.2 review log | — |
| 9.02.2(6) | "Program descriptive materials (course announcement information)." | 011 | `6-descriptive/` in the bundle: the public 8.01 payload exactly as served (`public_detail` is shared between the route and the bundle), the same rendered readable, every policy version, and the instructions page | — |
| 9.02 | "CPE program sponsors must retain adequate documentation (electronic or paper) for a minimum of five years to support their compliance with these Standards and the reports that may be required of participants." | 012 | Object storage on Spaces (`SpacesStorage` in `backend/app/storage.py`; private bucket; `packages/`, `certificates/`, `audits/` write-once); managed Postgres with daily snapshots; nightly logical dump to `backups/` with 90-day + monthly retention (`backend/app/services/backups.py`, `deploy/backup.sh`); restore drill recorded in `docs/OPERATIONS.md` | Spaces has no object versioning; write-once discipline is enforced by the application, not the bucket. Off-provider copy of backups is not implemented. |
| 4.05.2 | "Integral aspects of the learning environment that should be carefully monitored include the number of participants and the facilities and technologies employed in the delivery of the learning activity." | 012 | `/api/v1/health` (`backend/app/routers/health.py`: db, storage, ffprobe, last backup) with 503 on any failure; external uptime monitor on it (`docs/OPERATIONS.md`) | Monitoring is availability only; no load testing has been done. |
| 9.01 | Certificates of completion delivered to participants and retrievable (9.01.1: the sponsor issues and maintains them). | 012 | Certificates persist in `certificates/` on Spaces and are covered by the backup policy; participants re-download them through the API behind the session check | — |
| 9.02 | (correction, 2026-08-30, to the 012 row above) | 012 | The 012 row's gap "Spaces has no object versioning" was wrong: Spaces does offer object versioning, enable-able through the API only (the bucket Settings page shows the control). It is **not yet enabled** on `supercpe-prod-nyc3`, so the substance of the gap stands — write-once discipline is still enforced by the application, not the bucket — but the reason is a pending feature, not a platform limitation. | Enabling it is deferred to its own Phase B feature because it is not a quick toggle: with versioning on, deletes write markers instead of removing bytes, so `backups.py` pruning would stop reclaiming storage — it needs a lifecycle rule expiring noncurrent versions under `backups/` only, leaving `packages/`, `certificates/`, `audits/` permanent. The runtime Spaces key is Limited Access and cannot set versioning, lifecycle, or bucket policy; enabling requires a temporary Full Access key, used and then deleted. |
| 9.02 | "CPE program sponsors must retain adequate documentation (electronic or paper) for a minimum of five years to support their compliance with these Standards and the reports that may be required of participants." — superCPE reads durability as part of retaining: a record that exists at completion must still exist five years later even if the bucket is mis-handled. | 013 | Object versioning on the bucket, enabled once by `deploy/bucket-setup.py` (temporary Full Access key), keeps every prior version of every retained object; the one lifecycle rule expires only noncurrent versions under `backups/` after `BACKUP_NONCURRENT_DAYS` (`backend/app/constants/storage.py`) — `packages/`, `certificates/`, `audits/` versions are never expired. The control is enforced, not assumed: in `prod` the app refuses to boot while versioning is not `Enabled` (`ensure_bucket_versioning` in `backend/app/storage.py`, called from `backend/app/main.py`) and `/health` reports `bucket_versioning`, 503 on error (`backend/app/routers/health.py`). This closes the versioning gap the 012 row and its 2026-08-30 correction recorded. | The lifecycle read-back against the real bucket (and the recovery drill) is the operator's 013 acceptance walkthrough; until it is run, versioning is verified against moto only. |
| 9.02 | Same paragraph — read against provider-level failure: originals, snapshots, and nightly dumps all at DigitalOcean means an account lockout, region loss, or billing lapse takes the originals and every backup together. | 013 | Off-site copy at a second provider: `backend/app/services/offsite.py` mirrors each night's dump (plus `backups/LATEST`) and the retained prefixes to a second S3-compatible bucket, called by `deploy/backup.sh` via `python -m app.cli mirror-offsite` after the primary backup is stamped, so an off-site failure can never mask a primary one; nothing ever deletes off-site. Config is `OFFSITE_*`, all-or-nothing, and in `prod` the endpoint must not be DigitalOcean (`boot_violations` in `backend/app/config.py`). `/health` reports `last_offsite_backup_at` from `backups/OFFSITE`; staleness is the uptime monitor's alarm, never a 503. | Optional in `prod` by design — the missing-offsite state is reported, not refused, so the site stays up while a provider is chosen or replaced. Restoring *from* the off-site copy is documented in `docs/OPERATIONS.md` but its drill is a Phase C item. |
| 9.02.2 (1)–(7) | "Required documentation elements for self study programs. Evidence of compliance with responsibilities set forth under these Standards that is to be retained by CPE program sponsors of self study programs includes the following: …" | 013 | The audit bundle zips under `audits/` are the retained form of the seven-element set (011), and `audits/` is the first prefix the off-site mirror covers (`MIRRORED_PREFIXES` in `backend/app/constants/storage.py`; `mirror_prefix` in `backend/app/services/offsite.py`). | `packages/` — the program-materials originals behind element (7) — is **not** mirrored off-site: videos are large, and every exported zip also exists in video-tool's `dist/` on the machine that produced it. Recorded as a known gap (ROADMAP improvement note), not built. The bundles themselves retain the manifests, questions, and transcripts; only the video files rely on the primary bucket plus video-tool's copy. |
| 9.01 item 2 | "For self study and nano learning programs, a certificate supplied by the CPE program sponsor after satisfactory completion of a qualified assessment" — the evidence of completion the sponsor must supply and stand behind. | 013 | Certificates under `certificates/` are the second prefix the off-site mirror covers, so the evidence of completion survives bucket mishandling (versioning) and provider loss (off-site copy) for the 9.02 five years. | — |
| 9.02 | (correction, 2026-08-30, to the three 013 off-site rows above) | 013 | The off-site mirror is built and tested but **not configured**: the operator decided on 2026-08-30 not to set up a second-provider bucket at this time. `OFFSITE_*` is unset in production, `/health` reports `last_offsite_backup_at: null`, and nothing is mirrored anywhere. The three rows above describe the control as coded, not as running; the parts of their claims that depend on a second provider (surviving provider-level loss) are not in effect. | Until a provider is chosen and `OFFSITE_*` is set, the provider-level-failure exposure stands in full: originals, snapshots, and nightly dumps all live at DigitalOcean. The site runs anyway by design (missing-offsite is reported, never refused). Turning the control on is configuration plus one `backup.sh` run — no code. |
| 9.02 | (correction, 2026-08-30, to the 013 versioning row above) | 014a | The 013 row's `deploy/bucket-setup.py` no longer exists: 014a moved its logic into `python -m app.cli bucket-setup` (`backend/app/cli.py`) after the script proved unrunnable from the droplet on 2026-08-30 (no host Python, not in the api image, bind-mount broke its path bootstrap; and the runbook's "Full Access key" hid that only an All Permissions key can make bucket-configuration calls — the tool now names this on `AccessDenied`). The control itself — `ensure_bucket_versioning`, the prod boot refusal, `/health`'s `bucket_versioning` — is unchanged. New: `python -m app.cli preflight`, run by `deploy/deploy.sh` before migrations (and inherited by `rollback.sh`), turns a would-be boot refusal into a failed deploy with the old version still serving. | Unchanged from the 013 row: versioning and the lifecycle rule are verified against moto only until the operator's real run records its read-backs in `docs/OPERATIONS.md`. |
| 8.01 | "CPE program sponsors must provide descriptive materials that enable participants to assess the appropriateness of learning activities … must make the following information available in advance: 1) Learning objectives … 11) Official NASBA sponsor statement". | 015, 016 | Satisfied by design since 016: `missing_items` in `backend/app/services/disclosure.py` is the one named completeness check — every applicable item mapped to a stored fact, an item unusable when its source refuses (a stale 005 credit fails item 3 even though a number exists), item 11 counted only while `may_claim_registry` is true ("if an approved NASBA sponsor" is the Standard's own condition). `courses.publish` refuses with one 422 error per missing item, each naming its 8.01 number, so a course that cannot disclose completely cannot be published; `launch_findings` (`backend/app/services/readiness.py`) refuses `coming_soon → open` while no published course passes the check. The public payloads (`backend/app/routers/courses.py`) carry a named field for every applicable item, asserted by key-set test in `backend/tests/test_disclosure.py` — the exact inverse of 015's landing-payload test. While `site_mode` is `coming_soon`, the 015 landing page still deliberately discloses none of the items (partial disclosure reads as descriptive material) and the disclosure pages answer only sessions through the hidden login. | A published course that would now fail the check (dev only; production starts empty) is flagged on the admin course view, never auto-unpublished; its public payload refuses to render only when the credit is stale. |
| 8.01.1 | "the program sponsor must disclose the significant features of the program in advance (for example, through the use of brochures, websites …). The CPE program sponsor's registration and attendance policies and procedures must be formalized, published, and made available to participants and include refund and cancellation policies as well as complaint resolution policies." | 015 | The landing payload's `policies_published` is derived from the 011 policy versions (`missing_kinds` in `backend/app/services/policies.py`); the page footer links the policies pages only while all three kinds have a current version, and renders without them otherwise — it never fakes a link to an unpublished policy. | On production the policies are not yet published, so the footer link is absent; publishing them is part of preparing to open, not of this feature. The linked `/policies` page itself is still behind the 009 site gate while `coming_soon`. |
| 9.01 item 8 | (003's Registry-claim rule: while not a Registry sponsor, no response, page, or document may include the words "National Registry" or a sponsor ID; every surface that renders sponsor facts reads `may_claim_registry`.) | 015 | The coming-soon page is the **first public surface** under this rule: it reads `may_claim_registry` from `GET /api/v1/landing` and renders its Registry block only when true. `test_landing_never_mentions_the_registry_while_unclaimable` (`backend/tests/test_waiting_list.py`) fetches the rendered landing response and asserts "National Registry" is absent while the flag is false. | — |
| 8.01.1 | "The CPE program sponsor's registration and attendance policies and procedures must be formalized, published, and made available to participants…" — read against the registration flow itself: the form through which a participant registers must make the published policy available at the point of registration. | 017 | The `/register` page (`frontend/src/pages/Register/Register.jsx`) links the published registration/attendance policy on the `/policies` page beside the submit button — it links the 011 policy, it does not restate it. The flow cannot exist without the policy: `POST /api/v1/register` is public only at `open`, and the open flip already refuses while any policy kind is unpublished (011) or outbound email is unconfigured (017's `email_not_configured` block finding in `backend/app/services/readiness.py` — an open site must be able to send the verification email its form promises). | — |
| 9.02 | "CPE program sponsors must retain adequate documentation (electronic or paper) for a minimum of five years…" — the accounts rule (009): accounts anchor retained records, so they are deactivated, never deleted. | 017 | Self-registered accounts inherit deactivate-never-delete unchanged: registration creates ordinary `accounts` rows (`role=participant`, via `create_account` in `backend/app/services/auth.py`) with no new delete path, and re-registering a deactivated account's email neither reactivates it nor creates a second row — the address holder gets an email to contact the sponsor, and reactivation stays the deliberate 009 admin action (`backend/app/services/registration.py`). The `email_message` outbound log and `email_verification_tokens` are declared operational records, not CPE records (model docstrings), the same declaration as 015's waiting list; no `RETENTION_YEARS` linkage. | — |
| 8.01 item 9, 8.01.1 | (update, 2026-08-31, to the 011/016 items 8–11 row above) "9) Refund policy for courses sold for a fee or cancellation policy" — read now that courses actually are sold for a fee (018). | 018 | The `refund_policy` link on the course page (016) is load-bearing at the point of sale: the Enroll button and the linked published refund policy live on the same page, and checkout is impossible while the site is closed or the policy unpublished (the 016 publish gate and the 011/016/017/018 open gate both refuse first). A refund is executed in the Stripe dashboard; the signed `charge.refunded` webhook marks the payment row `refunded` and deliberately stops (`_handle_refunded` in `backend/app/services/payments.py`) — whether a refund unwinds access is the published policy's question, answered by the guarded, logged admin void action (`void` in `backend/app/services/enrollments.py`, surfaced on `/admin/payments` by the refunded-with-active-enrollment flag). | The refund policy's text is the sponsor's own; superCPE enforces that one is published and that the refund workflow stops where the policy begins, not what the policy says. |
| 9.02 | "CPE program sponsors must retain adequate documentation (electronic or paper) for a minimum of five years to support their compliance with these Standards" — noting that 9.02.2's element list for self study programs does not name payment or pricing records. | 018 | `payments` rows store the amount and currency per charge as Stripe reported them, never re-derived from the course's current price (a later price change cannot rewrite what anyone was charged); the rows are never deleted and outlive `RETENTION_YEARS` (docstring in `backend/app/models/payment.py`). | Deliberately beyond the letter: the Standard requires none of this — payment records are kept as financial records with their own clocks (tax, disputes), not as 9.02.2 CPE documentation, and the audit bundle does not include them. |
| 9.01 | "The documentation should be provided as soon as possible and should not exceed 60 days (so that participants can report their earned CPE credits in a timely manner)." | 019 | Documentation now reaches the participant without being asked: on completion, one `certificate` email with the PDF attached is sent strictly after the completion transaction commits (`deliver_after_completion` in `backend/app/services/delivery.py`, called by the passing submit in `backend/app/routers/my.py`), through the 017 email service extended with attachment support (`backend/app/services/email.py`; `email_message.attachment_filename` logs the filename, never the bytes). The on-demand download (010) is unchanged and is what actually satisfies the 60-day expectation — delivery failure cannot fail completion: `delivery_status`/`delivered_at` on `completions` record the outcome, a `failed` send is flagged on the admin completions view, and the guarded admin Resend (`POST /api/v1/admin/completions/{id}/resend`) is the recovery. | No automatic retries — one loud flag and a human button, by design. A completion whose render is blocked by sponsor issuance fields stays `pending` (the 010 `certificates_overdue` finding remains the 60-day backstop for that state). |
| 9.01.1 | "The CPE program sponsor is the individual or organization responsible for issuing the certificate of completion and maintaining the documentation required by these Standards. … CPE program sponsors must provide participants with documentation (electronic or paper) to support their claims of CPE credit." | 019 | The public verification page is the sponsor's channel for standing behind the documentation it issued: `GET /api/v1/certificates/verify/{code}` (`backend/app/routers/certificates.py`) confirms a certificate to anyone the participant hands it to — a state board, an employer — serving only the completion-time snapshot (name, course, field of study, credit, completion date, sponsor, "Self study"), never today's course row. The code is the 010 `verification_token` (256-bit, unique, indexed — an identifier, not a credential), printed on certificates from 019 on ("Verify this certificate at supercpe.com/certificates/verify — code: …", `backend/app/services/certificates.py`); the participant's page also offers the code and shareable link. Unknown and malformed codes answer one identical 404; the route obeys the 009 site gate (public at `open`) and is rate limited in Caddy. | Certificates rendered before 019 (dev only; production starts empty) print the old `/verify/{token}` URL — their stored PDFs are immutable — but their codes verify at the new page unchanged. |
| 7.01, 7.01.1 | (update, 2026-08-31, to the 005 7.01 row above) "rules and regulations of state boards of accountancy may differ on acceptable increments of CPE credit" … sponsors "may round down (but not up)" … "The CPA claiming the CPE credits should refer to the respective state board requirements regarding acceptability of one-fifth and one-half CPE credits." | 020 | superCPE still awards in one-fifth increments — 005's stored credit and `round_down` are untouched. What a verified board increment means for that award is surfaced per viewer: `jurisdiction_policies` rows the admin verified against a named source on a date (`backend/app/services/jurisdictions.py`), served by `GET /courses/{code}/jurisdiction-note` with 7.01.1's round-down computed per request (`board_rounded`, never stored) and a fixed final-authority sentence (`FINAL_AUTHORITY_SENTENCE` in `backend/app/constants/jurisdiction_policy.py`). Non-technical caps (2024 Fields of Study classification in `backend/app/constants/fields_of_study.py`) are quoted, never computed. The certificate is pinned jurisdiction-free by test. | The hint is a courtesy lookup, not compliance-by-proxy: the claiming CPA retains the Standard's own duty to check with their board, the table ships empty until Dane verifies rows, and superCPE never tracks reporting-period totals against a cap — it cannot know a CPA's other CPE. |
| 8.01 | (update, 2026-08-31, to the 015/016 8.01 row above) "must provide descriptive materials … available in advance" — read against the one email the waiting list is sent (021). | 021 | The invitation follows the landing page's rule now that full disclosure exists to link to: **link, don't restate**. `render_invitation` in `backend/app/services/invitations.py` names the course in one plain sentence and links the course page (where 016's complete 8.01 disclosure lives) and the register page — no credit figure, no field of study, no level, no price, and no "National Registry" whether claimable or not (pinned by test in `backend/tests/test_invitations.py`). The send refuses while `coming_soon`, so the linked pages exist before any email points at them, and each entry is invited at most once (`invitation_status` on `waiting_list`, one-shot by construction). | The invitation is not a descriptive material and holds itself to disclosing nothing; the course page it links is the 8.01 surface. Waiting-list rows remain not CPE records. |
| 8.01 | (update, 2026-08-31, to the 015/016 8.01 row above) "must provide descriptive materials … available in advance" — read against the site-wide metadata scrapers and search engines see (022). | 022 | The static OG/meta tags in `frontend/index.html` follow the landing page's and the invitation's rule: **link, don't restate**. The site-wide description describes the sponsor, not the course — no credit figure, no field of study, no level, no price, no "QAS", and no "National Registry", pinned by test (`test_index_html_obeys_the_content_rules` in `backend/tests/test_identity.py`, which renders the file exactly as the build does). The JSON-LD Organization block carries only name, url, and logo. The mode-aware `/sitemap.xml` (`sitemap_paths` in `backend/app/services/site.py`) lists only the root while `coming_soon` and only renderable published course pages at `open` — a page that cannot disclose completely is never announced to an index. | Per-course OG cards would need SSR and would reopen the 8.01 question per URL; deliberately not built (ROADMAP improvement note). The tags are site-wide, so a pasted course URL previews with the sponsor card. |
